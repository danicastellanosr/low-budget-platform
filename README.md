# LOW Budget Platform

A standalone HTML budget planning platform for Laura to estimate move-related expenses, compare savings goals, and review live expense reports.

Open `index.html` directly or deploy it as a static site.
